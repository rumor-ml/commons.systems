- config can update permissions in claude settings.json
- config can setup claude.md
  - claude always runs tests
  - use e2e testing framework from server project
- server refactor is complete

  - ::1:: server can run boilerplate
    ==========================

    - assistant manages new server

    - ::2:: nixconfig can deploy server (rename to deploy)
      ========

  - ::3:: log package saves logs to sqlite db
    ====

    - log tool loaded from logmcp
      - all projects are updated to use log tool
  - reusable e2e test server
  - assistant can use discovery from config project
- user can update config from assistant
- update finances
- assistant launches claude in correct nix dev shell
- assistant configures tmux to split panes with claude
