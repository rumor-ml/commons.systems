---
description: Create a new git worktree with a branch name based on task description
model: haiku
---

1. Generate a concise, descriptive branch name based on the task description: "{{args}}"
2. Create the worktree in ~/worktrees
3. After creating the worktree, set the upstream branch to origin/<branch-name>.
