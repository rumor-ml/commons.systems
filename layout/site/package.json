{
  "name": "@commons.systems/layout",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "! grep -r 'console.log' src/ || (echo 'Found console.log statements' && exit 1)"
  },
  "dependencies": {
    "firebase": "^10.7.1",
    "jspdf": "^2.5.2",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
